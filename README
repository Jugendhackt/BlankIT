Hallo!
David Schmdit
Levy Ehrstein


DB server:
192.168.173.28
User: jh
Password: -leer-
DB name: WC

select D.ID,D.Wort,W.anz from dict as D join worte as W on D.ID=W.ID AND 
D.Wort = ?;

Beton kann außerdem Betonzusatzstoffe und Betonzusatzmittel enthalten , 
die die Eigenschaften des Baustoffs gezielt beeinflussen.


function sanitizeString($inp, $replaceEnter = true)
{
	/*** memove UNICODE to prevent a multi-byte charset attack vector ***/
	$inp = mb_convert_encoding ($inp, 'UTF-8');
	/*** sanitize the string ***/
	$inp = filter_var($inp, FILTER_SANITIZE_FULL_SPECIAL_CHARS);
	if ($replaceEnter)
		$inp = str_replace("\n", "<br/>", $inp);

	return $inp;
}

